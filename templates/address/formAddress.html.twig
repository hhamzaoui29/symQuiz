{% extends "base.html.twig" %}

{% block body %}

<form class = "form-container" method="POST">
    <h1> form </h1>
    <hr>
    {{ form_start(form) }}
      <div class="form-group">

                        {{ form_label(form.number, 'N°') }}
                        {{ form_widget(form.number, {'attr': {'class': 'form-input'}}) }}
                    </div>

                    <div class="form-group">
                        {{ form_label(form.type, 'Street guy') }}
                        {{ form_widget(form.type, {'attr': {'class': 'form-input'}}) }}
                    </div>

                    <div class="form-group">
                        {{ form_label(form.name, 'Name of the street') }}
                        {{ form_widget(form.name, {'attr': {'class': 'form-input'}}) }}
                    </div>

                    <div class="form-group">
                        {{ form_label(form.postcode, 'Postcode') }}
                        {{ form_widget(form.postcode, {'attr': {'class': 'form-input'}}) }}
                    </div>

                    <div class="form-group">
                        {{ form_label(form.country, 'Country') }}
                        {{ form_widget(form.country, {'attr': {'class': 'form-input'}}) }}
                    </div>

                    
    <div class="links">
    <button type="submit" class="form-submit">Save</button>
    <a href="/addresses">List Addresses</a>
    </di>

    {{ form_end(form) }}

</form>

{% endblock body %}
